# рантайм / кэш
.claude/
__pycache__/
*.pyc
*.log

# тяжёлые обучаемые артефакты — не идут в git (см. README, «Структура»)
models/*.pt
models/grid/*.pt
report/
second_date/
# дампы предсказаний — воспроизводятся bench_run.py по чекпойнтам models/,
# слишком тяжёлые для обычного git (см. metrics_*.csv рядом — числа из них уже сохранены)
data/dump_*.npz
# годовые минутные ряды станций — пересобираются из INTERMAGNET XML одной
# командой (bench_prep.py --code <код> --years ...); бинарные, ~1.5 МБ на год,
# 6 станций × 13 лет — держать рядом с исходными XML, не в git
data/*_20??.npz

# копия полученных рисунков для просмотра/пересылки (оригиналы — в figures/)
pictures/
